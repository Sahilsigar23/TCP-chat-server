# Chat Server Environment Variables
# Copy this file and rename it to .env (optional)
# Or set the PORT environment variable directly in your shell

# Server Port (defaults to 4000 if not set)
PORT=4000

# Example: To use port 5000, set:
# PORT=5000

